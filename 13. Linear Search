#User function Template for python3

class Solution:
    #Complete the below function
    def search(self,arr, x):
        try:
            return arr.index(x)
        except ValueError:
            return -1
