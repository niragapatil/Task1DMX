
from typing import List


class Solution:
    def largest(self, arr : List[int]) -> int:
        maximum = arr[0]
        for elem in arr:
            if elem > maximum:
                maximum = elem
        return(maximum)
        

