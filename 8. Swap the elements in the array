#User function Template for python3

class Solution:
	def swapElements(self, arr, n):
	    for i in range(len(arr)-2):
	        temp = arr[i]
            arr[i] = arr[i+2]
            arr[i+2] = temp
